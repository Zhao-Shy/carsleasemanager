<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.threemusketeers.carsleasemanager.mapper.UserMapperExtend" >
  <resultMap id="user" type="com.threemusketeers.carsleasemanager.entity.User" extends="com.threemusketeers.carsleasemanager.mapper.UserMapper.BaseResultMap"></resultMap>
  <sql id="Base_Column_List" >
    id, username, password, user_type, name, moblie, address, occupation, id_card_img, 
    driver_license_img, member_type, create_by, create_date, update_by, update_date, 
    remarks, del_flag
  </sql>

  <select id="login" resultMap="user" >
    select
        username,
        user_type,
        member_type
    from sys_user
    where username = #{user.username}
    and password = #{user.password}
  </select>

</mapper>